reset()
#---------------------------------------------------------------------
#the following parameters must be set-up before launching this script:
hi_prime = 13
hi_cfs = 30
poly_num = 20
start = 0

load('genRatRoot.sage')
load('genfiles.sage')
#---------------------------------------------------------------------
reset()
